let sbar = document.querySelector("d-flex");

let ctitles = document.querySelectorAll(".this");
console.log(ctitles);

ctitles[0].style.backgroundColor="";

let ctitle = document.querySelectorAll('input[placeholder="Search"]');
console.log(ctitle);

ctitle[0].style.color="yellow";